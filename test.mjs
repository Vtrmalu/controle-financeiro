import { initializeApp } from 'firebase/app'; import { getFirestore, collection, getDocs } from 'firebase/firestore'; const app = initializeApp({ apiKey: 'AIzaSyA9v2ItA82IEYUjFB-sDEP_QdR0GhnGGYs', projectId: 'controle-financeiro-4b59c' }); const db = getFirestore(app); try { const snap = await getDocs(collection(db, 'transactions')); console.log('Transactions in DB:', snap.size); } catch (e) { console.error('Error:', e); } process.exit(0);
